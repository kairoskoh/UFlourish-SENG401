@model JokesWebApp.Models.RefillPrescriptions

@{
    ViewData["Title"] = "Details";
}

<h1>Refill Request Details</h1>

<div>
    <h4>  </h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            Requested Patient
            @*@Html.DisplayNameFor(model => model.RefillDate)*@
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.UserName)
        </dd>
        <dt class = "col-sm-2">
            Medicine Name
            @*@Html.DisplayNameFor(model => model.MedicineName)*@
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.MedicineName)
        </dd>
        <dt class = "col-sm-2">
            Requested Date
            @*@Html.DisplayNameFor(model => model.RefillDate)*@
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.RequestRefillDate)
        </dd>
        <dt class="col-sm-2">
            @if (ViewData["email"].Equals("admin123@gmail.com"))
            {
                <text> Admin Access: </text>

            }
        </dt>
        <dd class="col-sm-10">
        </dd>
        <dt class="col-sm-2">
            @if (ViewData["email"].Equals("admin123@gmail.com"))
            {
                <input type="button" value="Approve Request" onclick="location.href=" class="btn btn-success"/>
            }
        </dt>
        <dd class="col-sm-10">
            @if (ViewData["email"].Equals("admin123@gmail.com"))
            {
                <input type="button" value="Decline Request" onclick="location.href=" class="btn btn-danger" />
            }
        </dd>
    </dl>
</div>


<div>
    @*<a asp-action="Edit" asp-route-id="@Model.Id">Edit</a> |*@
    <a asp-action="Index">Back to List</a>
</div>
